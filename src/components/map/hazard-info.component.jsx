import React from 'react';

import {
  IonTitle,
  IonToolbar
} from '@ionic/react';
const HazardInfo = ({ hazard }) => {
  return (
    <IonToolbar>
      <IonTitle>Footer</IonTitle>
    </IonToolbar>
  );
};

export default HazardInfo;
